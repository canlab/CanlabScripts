#!/usr/bin/env python3

#This is the "canlabulator", a program for rearranging directories into the canlab data format.

#Right now it's pretty unstable, so make sure to use the "shadow" program to make a shadow of the directory
#you want to canlabulate before running this script.

import os
import re